---
const waitlistEndpoint = import.meta.env.PUBLIC_WAITLIST_ENDPOINT ?? '/api/waitlist';
const navLinks = [
  ['#como-funciona', 'Cómo funciona'],
  ['#quedadas', 'Quedadas'],
  ['#tu-crew', 'Tu crew'],
];
const features = [
  {
    tone: 'navy',
    title: 'Crea quedadas en 30 segundos',
    body: 'Punto de encuentro, ritmo, cupo y nivel. Todo en un formulario. Sin copiar y pegar en cinco chats.',
    tags: ['Social', 'Workout', 'Long Run', 'Intervals'],
    icon: 'calendar',
  },
  {
    tone: 'lime',
    title: 'RSVP y waitlist automática',
    body: 'Los miembros se apuntan con un toque. Cuando se llena el cupo, la waitlist se gestiona sola.',
    tags: ['RSVP', 'Cupo', 'Waitlist'],
    icon: 'users',
  },
  {
    tone: 'navy',
    title: 'Comparte por WhatsApp',
    body: 'Genera un link de tu quedada para compartir. Los nuevos se apuntan directamente, sin preguntar en el chat.',
    tags: ['Link', 'WhatsApp', 'Sin caos'],
    icon: 'share',
  },
];
const trustChips = [
  ['mail', 'Sin spam', 'Solo novedades'],
  ['key', 'Acceso anticipado', 'Sé de los primeros'],
  ['heart', 'Hecho por runners', 'para admins de crews'],
];
const checklist = [
  'Crea quedadas en menos de 30 segundos',
  'RSVP y waitlist automática sin esfuerzo',
  'Comparte por WhatsApp sin perder el contexto',
];
const footerChips = [
  ['mail', 'Sin spam, lo prometemos'],
  ['shield', 'Acceso anticipado asegurado'],
  ['heart', 'Hecho por y para la comunidad runner'],
];
const heroShots = [
  { src: '/screenshots/home.png', alt: 'Pantalla principal de AppRunners' },
  { src: '/screenshots/communities.png', alt: 'Lista de comunidades en AppRunners' },
];
const showcaseShots = [
  { src: '/screenshots/community-detail.png', alt: 'Detalle de una comunidad' },
  { src: '/screenshots/new-community.png', alt: 'Crear nueva comunidad' },
  { src: '/screenshots/pace.png', alt: 'Configurar tu ritmo de running' },
];
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
    <meta name="theme-color" content="#15324A" />
    <meta
      name="description"
      content="AppRunners es la app para organizar tu crew de running. Publica quedadas, gestiona miembros y llena tus salidas."
    />
    <link rel="icon" type="image/png" href="/favicon.png" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&family=Space+Grotesk:wght@600;700&display=swap"
      rel="stylesheet"
    />
    <title>AppRunners · Organiza tu crew de running</title>
  </head>
  <body>
    <div class="page">
      <header class="nav">
        <a class="brand" href="#top" aria-label="AppRunners">
          <span class="brand__badge" aria-hidden="true">
            <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="17" cy="4" r="2" />
              <path d="M15.5 8.5 12 11l-2.5 3L11 18l-3 4" />
              <path d="M8 10.5 5.5 13 8 16" />
              <path d="m13 13 3 2 3-1" />
            </svg>
          </span>
          <span class="brand__name">AppRunners</span>
        </a>
        <nav class="nav__links" aria-label="Navegación principal">
          {navLinks.map(([href, label]) => <a href={href}>{label}</a>)}
        </nav>
        <a class="btn btn--primary nav__cta" href="#apuntate">Quiero enterarme</a>
      </header>

      <section class="hero" id="top">
        <div class="hero__content">
          <span class="pill">Próximamente</span>
          <h1>
            Tu crew merece algo mejor
            <span class="accent">que un grupo de WhatsApp.</span>
          </h1>
          <p class="hero__lead">
            Publica quedadas, gestiona miembros y controla el RSVP sin el caos del chat.
            Pensado para admins de crews locales de 15 a 80 personas.
          </p>

          <form class="capture" id="apuntate" action={waitlistEndpoint} method="post" data-waitlist-form novalidate>
            <h2 class="capture__title">Apúntate para ser el primero</h2>
            <p class="capture__sub">Te avisamos en cuanto abramos el acceso.</p>
            <input type="hidden" name="source" value="landing-hero" />
            <div class="capture__row">
              <label class="capture__input">
                <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                  <rect x="3" y="5" width="18" height="14" rx="2" />
                  <path d="m3 7 9 6 9-6" />
                </svg>
                <input
                  type="email"
                  name="email"
                  placeholder="Tu correo electrónico"
                  autocomplete="email"
                  inputmode="email"
                  required
                />
              </label>
              <button class="btn btn--primary" type="submit">Quiero enterarme</button>
            </div>
            <p class="capture__status" data-waitlist-status role="status" aria-live="polite"></p>
            <p class="capture__foot">
              <span aria-hidden="true">🇪🇸</span> Lanzamiento inicial en España · Acceso
              anticipado próximamente
            </p>
          </form>

          <ul class="trust" aria-label="Por qué apuntarte">
            {trustChips.map(([icon, title, desc]) => (
              <li>
                <span class="trust__icon" aria-hidden="true">
                  {icon === 'mail' && (
                    <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="5" width="18" height="14" rx="2" /><path d="m3 7 9 6 9-6" /></svg>
                  )}
                  {icon === 'key' && (
                    <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="8" cy="15" r="4" /><path d="m10.6 12.4 8.4-8.4 2 2-2 2 2 2-2 2-2-2-2 2" /></svg>
                  )}
                  {icon === 'heart' && (
                    <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20.8 5.6a5.5 5.5 0 0 0-7.8 0L12 6.6l-1-1a5.5 5.5 0 1 0-7.8 7.8l1 1L12 22l7.8-7.6 1-1a5.5 5.5 0 0 0 0-7.8z" /></svg>
                  )}
                </span>
                <div>
                  <strong>{title}</strong>
                  <span>{desc}</span>
                </div>
              </li>
            ))}
          </ul>
        </div>

        <div class="hero__visual" aria-hidden="true">
          {heroShots.map((s, i) => (
            <div class={`shot shot--${i === 0 ? 'front' : 'back'}`}>
              <img src={s.src} alt={s.alt} loading="eager" />
            </div>
          ))}
        </div>
      </section>

      <section class="features" id="como-funciona">
        {features.map((f) => (
          <article class="feature">
            <span class={`feature__icon feature__icon--${f.tone}`} aria-hidden="true">
              {f.icon === 'users' && (
                <svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="9" cy="8" r="3.5" /><circle cx="17" cy="10" r="2.5" /><path d="M3 19c.8-3 3-4.5 6-4.5s5.2 1.5 6 4.5" /><path d="M15 19c.5-2 2-3 4-3s3.5 1 4 3" /></svg>
              )}
              {f.icon === 'calendar' && (
                <svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3.5" y="5" width="17" height="15" rx="2" /><path d="M3.5 10h17M8 3.5v3M16 3.5v3" /></svg>
              )}
              {f.icon === 'share' && (
                <svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="18" cy="5" r="3" /><circle cx="6" cy="12" r="3" /><circle cx="18" cy="19" r="3" /><path d="m8.59 13.51 6.83 3.98M15.41 6.51 8.59 10.49" /></svg>
              )}
            </span>
            <h3>{f.title}</h3>
            <p>{f.body}</p>
            <div class="feature__tags">
              {f.tags.map((t) => <span>{t}</span>)}
            </div>
          </article>
        ))}
      </section>

      <section class="showcase" id="quedadas">
        <div class="showcase__phones" aria-hidden="true">
          {showcaseShots.map((s, i) => (
            <div class={`shot shot--sm${i === 1 ? ' shot--raised' : ''}`}>
              <img src={s.src} alt={s.alt} loading="lazy" />
            </div>
          ))}
        </div>

        <div class="showcase__copy">
          <p class="eyebrow eyebrow--muted">Diseñada para admins de crews</p>
          <h2 id="tu-crew">
            Para que coordinar tu grupo <span class="highlight">deje de ser un trabajo.</span>
          </h2>
          <p>
            Usa Strava para registrar la carrera. Usa AppRunners para organizar la quedada.
          </p>
          <ul class="check">
            {checklist.map((item) => (
              <li>
                <span class="check__mark" aria-hidden="true">
                  <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="m5 12 4 4 10-10" /></svg>
                </span>
                {item}
              </li>
            ))}
          </ul>
        </div>
      </section>

      <section class="final" id="comunidades">
        <div class="final__inner">
          <div class="final__copy">
            <span class="pill">Próximamente</span>
            <h2>Sé de los primeros en organizar tu crew con AppRunners.</h2>
            <p>Apúntate y te avisaremos en cuanto abramos el acceso anticipado.</p>
          </div>
          <form class="final__form" action={waitlistEndpoint} method="post" data-waitlist-form novalidate>
            <input type="hidden" name="source" value="landing-footer" />
            <label class="capture__input capture__input--dark">
              <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="5" width="18" height="14" rx="2" /><path d="m3 7 9 6 9-6" /></svg>
              <input
                type="email"
                name="email"
                placeholder="Tu correo electrónico"
                autocomplete="email"
                inputmode="email"
                required
              />
            </label>
            <button class="btn btn--primary" type="submit">Notifícame</button>
            <p class="capture__status" data-waitlist-status role="status" aria-live="polite"></p>
          </form>
        </div>
        <ul class="final__chips">
          {footerChips.map(([icon, label]) => (
            <li>
              <span aria-hidden="true">
                {icon === 'mail' && (
                  <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="5" width="18" height="14" rx="2" /><path d="m3 7 9 6 9-6" /></svg>
                )}
                {icon === 'shield' && (
                  <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2 4 6v6c0 5 3.5 9 8 10 4.5-1 8-5 8-10V6l-8-4z" /></svg>
                )}
                {icon === 'heart' && (
                  <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20.8 5.6a5.5 5.5 0 0 0-7.8 0L12 6.6l-1-1a5.5 5.5 0 1 0-7.8 7.8l1 1L12 22l7.8-7.6 1-1a5.5 5.5 0 0 0 0-7.8z" /></svg>
                )}
              </span>
              {label}
            </li>
          ))}
        </ul>
      </section>
    </div>
    <script>
      const waitlistForms = document.querySelectorAll('[data-waitlist-form]');

      waitlistForms.forEach((form) => {
        form.addEventListener('submit', async (event) => {
          event.preventDefault();

          const currentForm = event.currentTarget;
          if (!(currentForm instanceof HTMLFormElement)) {
            return;
          }

          const status = currentForm.querySelector('[data-waitlist-status]');
          const submitButton = currentForm.querySelector('button[type="submit"]');
          const formData = new FormData(currentForm);

          if (submitButton instanceof HTMLButtonElement) {
            submitButton.disabled = true;
            submitButton.dataset.originalText = submitButton.textContent ?? '';
            submitButton.textContent = 'Guardando...';
          }

          if (status instanceof HTMLElement) {
            status.textContent = '';
            status.dataset.state = '';
          }

          try {
            const response = await fetch(currentForm.action, {
              body: formData,
              method: 'POST',
            });
            const result = await response.json().catch(() => null);
            const message = result?.message ?? (
              response.ok
                ? 'Te avisaremos cuando abramos el acceso.'
                : 'No hemos podido guardar tu correo. Inténtalo de nuevo.'
            );

            if (!response.ok) {
              throw new Error(message);
            }

            currentForm.reset();

            if (status instanceof HTMLElement) {
              status.textContent = message;
              status.dataset.state = 'success';
            }
          } catch (error) {
            if (status instanceof HTMLElement) {
              status.textContent = error instanceof Error
                ? error.message
                : 'No hemos podido guardar tu correo. Inténtalo de nuevo.';
              status.dataset.state = 'error';
            }
          } finally {
            if (submitButton instanceof HTMLButtonElement) {
              submitButton.disabled = false;
              submitButton.textContent = submitButton.dataset.originalText ?? 'Enviar';
            }
          }
        });
      });
    </script>
  </body>
</html>

<style>
  :root {
    --font-display: 'Space Grotesk', ui-sans-serif, system-ui, sans-serif;
    --background: #F6F7FB;
    --surface: #FFFFFF;
    --hero: #15324A;
    --hero-2: #0B1C2B;
    --hero-text: #FFFFFF;
    --hero-text-muted: #D9E6F2;
    --hero-accent: #C7F464;
    --border: #E1E7EF;
    --text: #15324A;
    --muted-text: #5C6B7E;
    --tint: #C7F464;
    --tint-dark: #B3DF4A;
    --on-tint: #15324A;
    --success: #6C8F25;
    --chip: #F1FAD1;
    --card: #FFFFFF;
  }

  * {
    box-sizing: border-box;
  }

  html {
    scroll-behavior: smooth;
    color-scheme: light;
  }

  body {
    margin: 0;
    min-width: 320px;
    font-family: 'Inter', ui-sans-serif, system-ui, -apple-system, 'Segoe UI', sans-serif;
    background: var(--background);
    color: var(--text);
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    -webkit-text-size-adjust: 100%;
  }

  h1, h2, h3, h4, p {
    margin: 0;
  }

  h1, h2, h3 {
    font-family: var(--font-display);
    font-weight: 800;
  }

  a {
    color: inherit;
  }

  img {
    max-width: 100%;
    height: auto;
    display: block;
  }

  .page {
    max-width: 1180px;
    margin: 0 auto;
    padding: 0.75rem;
  }

  /* ---------- Header ---------- */
  .nav {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.35rem 0.25rem 1rem;
  }

  .brand {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-family: var(--font-display);
    font-weight: 800;
    font-size: 1.05rem;
    text-decoration: none;
    color: var(--text);
  }

  .brand__badge {
    display: inline-grid;
    place-items: center;
    width: 32px;
    height: 32px;
    border-radius: 10px;
    background: var(--hero);
    color: var(--tint);
  }

  .nav__links {
    display: none;
    gap: 1.4rem;
    margin-inline: auto;
    font-size: 0.92rem;
    font-weight: 500;
    color: var(--muted-text);
  }

  .nav__links a {
    text-decoration: none;
    transition: color 160ms ease;
  }

  .nav__links a:hover {
    color: var(--hero);
  }

  .nav__cta {
    margin-left: auto;
    padding: 0.55rem 0.95rem;
    font-size: 0.88rem;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    border: 0;
    border-radius: 999px;
    padding: 0.85rem 1.2rem;
    font: inherit;
    font-weight: 700;
    cursor: pointer;
    text-decoration: none;
    transition: transform 160ms ease, background-color 160ms ease, box-shadow 160ms ease;
    white-space: nowrap;
    min-height: 44px;
  }

  .btn--primary {
    background: var(--tint);
    color: var(--on-tint);
    box-shadow: 0 10px 24px rgba(199, 244, 100, 0.3);
  }

  .btn--primary:hover,
  .btn--primary:active {
    background: var(--tint-dark);
  }

  .btn--primary:hover {
    transform: translateY(-1px);
  }

  .btn:disabled {
    cursor: wait;
    opacity: 0.72;
    transform: none;
  }

  /* ---------- Hero ---------- */
  .hero {
    position: relative;
    border-radius: 22px;
    padding: 1.5rem 1.25rem 2rem;
    background: radial-gradient(120% 90% at 0% 0%, #1e4a6e 0%, var(--hero) 45%, var(--hero-2) 100%);
    color: var(--hero-text);
    overflow: hidden;
    isolation: isolate;
  }

  .hero::before {
    content: '';
    position: absolute;
    inset: 0;
    background:
      radial-gradient(60% 40% at 100% 0%, rgba(199, 244, 100, 0.12), transparent 60%),
      radial-gradient(40% 40% at 10% 100%, rgba(255, 255, 255, 0.04), transparent 60%);
    z-index: 0;
    pointer-events: none;
  }

  .hero__content {
    position: relative;
    z-index: 1;
  }

  .pill {
    display: inline-flex;
    align-items: center;
    padding: 0.3rem 0.75rem;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.1);
    color: var(--hero-accent);
    font-family: var(--font-display);
    font-size: 0.66rem;
    font-weight: 800;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .hero h1 {
    margin: 1rem 0 0.9rem;
    font-size: 2.25rem;
    line-height: 1.05;
    letter-spacing: -0.02em;
    font-weight: 800;
  }

  .accent {
    color: var(--tint);
  }

  .highlight {
    color: var(--hero);
  }

  .hero__lead {
    margin-bottom: 1.4rem;
    color: var(--hero-text-muted);
    font-size: 1rem;
    line-height: 1.5;
  }

  .capture {
    padding: 1rem;
    border-radius: 16px;
    background: rgba(7, 18, 28, 0.55);
    border: 1px solid rgba(255, 255, 255, 0.08);
    backdrop-filter: blur(6px);
  }

  .capture__title {
    font-size: 1rem;
    font-weight: 800;
    margin-bottom: 0.2rem;
  }

  .capture__sub {
    color: var(--hero-text-muted);
    font-size: 0.85rem;
    margin-bottom: 0.85rem;
  }

  .capture__row {
    display: grid;
    gap: 0.55rem;
  }

  .capture__input {
    display: flex;
    align-items: center;
    gap: 0.55rem;
    padding: 0.8rem 0.9rem;
    border-radius: 12px;
    background: rgba(255, 255, 255, 0.08);
    border: 1px solid rgba(255, 255, 255, 0.12);
    color: var(--hero-text-muted);
    min-height: 48px;
  }

  .capture__input input {
    flex: 1;
    min-width: 0;
    border: 0;
    background: transparent;
    color: var(--hero-text);
    font: inherit;
    outline: none;
    font-size: 16px; /* evita zoom iOS */
  }

  .capture__input input::placeholder {
    color: rgba(255, 255, 255, 0.45);
  }

  .capture__foot {
    margin-top: 0.75rem;
    color: var(--hero-text-muted);
    font-size: 0.78rem;
    line-height: 1.4;
  }

  .capture__status {
    min-height: 1.2rem;
    margin-top: 0.65rem;
    color: var(--hero-text-muted);
    font-size: 0.82rem;
    font-weight: 700;
    line-height: 1.4;
  }

  .capture__status[data-state='success'] {
    color: var(--hero-accent);
  }

  .capture__status[data-state='error'] {
    color: #ffb19f;
  }

  .trust {
    list-style: none;
    margin: 1.4rem 0 0;
    padding: 0;
    display: grid;
    gap: 0.7rem;
  }

  .trust li {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    font-size: 0.82rem;
    color: var(--hero-text-muted);
  }

  .trust strong {
    display: block;
    color: var(--hero-text);
    font-size: 0.85rem;
  }

  .trust__icon {
    display: inline-grid;
    place-items: center;
    width: 30px;
    height: 30px;
    border-radius: 8px;
    background: rgba(255, 255, 255, 0.08);
    color: var(--hero-accent);
    flex-shrink: 0;
  }

  /* ---------- Screenshots ---------- */
  .hero__visual {
    position: relative;
    z-index: 1;
    display: flex;
    justify-content: center;
    gap: 0.6rem;
    margin-top: 1.75rem;
    perspective: 1200px;
  }

  .shot {
    position: relative;
    flex: 0 0 auto;
    width: min(48%, 200px);
    border-radius: 26px;
    background: #050b0d;
    padding: 6px;
    box-shadow:
      0 24px 48px rgba(0, 0, 0, 0.45),
      0 0 0 1px rgba(255, 255, 255, 0.04);
    overflow: hidden;
  }

  .shot img {
    border-radius: 20px;
    width: 100%;
    display: block;
    aspect-ratio: 9 / 19.5;
    object-fit: cover;
    background: #0B1C2B;
  }

  .shot--front {
    transform: rotate(-2deg);
    z-index: 2;
  }

  .shot--back {
    transform: translateY(12px) rotate(3deg);
  }

  /* ---------- Features ---------- */
  .features {
    display: grid;
    grid-template-columns: 1fr;
    gap: 0.75rem;
    margin: 1rem 0;
  }

  .feature {
    padding: 1.25rem;
    border-radius: 18px;
    background: var(--card);
    border: 1px solid var(--border);
    text-align: center;
  }

  .feature__icon {
    display: inline-grid;
    place-items: center;
    width: 52px;
    height: 52px;
    border-radius: 50%;
    margin-bottom: 0.85rem;
  }

  .feature__icon--navy {
    background: var(--hero);
    color: var(--tint);
  }

  .feature__icon--lime {
    background: var(--tint);
    color: var(--on-tint);
  }

  .feature h3 {
    font-size: 1.1rem;
    font-weight: 800;
    margin-bottom: 0.35rem;
  }

  .feature p {
    color: var(--muted-text);
    font-size: 0.92rem;
    margin-bottom: 0.9rem;
    line-height: 1.45;
  }

  .feature__tags {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 0.3rem;
  }

  .feature__tags span {
    display: inline-flex;
    padding: 0.25rem 0.6rem;
    border-radius: 999px;
    background: var(--chip);
    color: var(--muted-text);
    font-size: 0.72rem;
    font-weight: 700;
  }

  /* ---------- Showcase ---------- */
  .showcase {
    display: grid;
    gap: 1.25rem;
    padding: 1.5rem 0.25rem;
  }

  .showcase__phones {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 0.5rem;
    align-items: end;
  }

  .shot--sm {
    width: 100%;
    border-radius: 18px;
    padding: 4px;
  }

  .shot--sm img {
    border-radius: 14px;
  }

  .shot--raised {
    transform: translateY(-12px);
  }

  .eyebrow--muted {
    display: inline-block;
    color: var(--muted-text);
    font-size: 0.7rem;
    font-weight: 800;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    margin-bottom: 0.7rem;
  }

  .showcase__copy h2 {
    font-size: 1.8rem;
    line-height: 1.1;
    letter-spacing: -0.02em;
    margin-bottom: 0.75rem;
  }

  .showcase__copy > p {
    color: var(--muted-text);
    font-size: 0.98rem;
    line-height: 1.55;
    margin-bottom: 1.1rem;
  }

  .check {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 0.55rem;
  }

  .check li {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    font-size: 0.95rem;
  }

  .check__mark {
    display: inline-grid;
    place-items: center;
    width: 22px;
    height: 22px;
    border-radius: 50%;
    background: rgba(108, 143, 37, 0.15);
    color: var(--success);
    flex-shrink: 0;
  }

  /* ---------- Final CTA ---------- */
  .final {
    border-radius: 22px;
    padding: 1.75rem 1.25rem;
    background: radial-gradient(120% 90% at 100% 0%, #1e4a6e, var(--hero) 60%, var(--hero-2));
    color: var(--hero-text);
    margin-bottom: 1rem;
  }

  .final__inner {
    display: grid;
    gap: 1.25rem;
  }

  .final__copy h2 {
    margin: 0.65rem 0 0.35rem;
    font-size: 1.6rem;
    letter-spacing: -0.02em;
    line-height: 1.15;
  }

  .final__copy p {
    color: var(--hero-text-muted);
    font-size: 0.95rem;
  }

  .final__form {
    display: grid;
    gap: 0.55rem;
  }

  .final__form .capture__status {
    margin-top: 0;
  }

  .capture__input--dark {
    background: rgba(255, 255, 255, 0.06);
  }

  .final__chips {
    list-style: none;
    padding: 1rem 0 0;
    margin: 1.25rem 0 0;
    border-top: 1px solid rgba(255, 255, 255, 0.08);
    display: grid;
    gap: 0.75rem;
    color: var(--hero-text-muted);
    font-size: 0.85rem;
  }

  .final__chips li {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
  }

  .final__chips li span {
    color: var(--hero-accent);
    display: inline-flex;
  }

  /* ---------- Tablet / desktop ---------- */
  @media (min-width: 720px) {
    .page {
      padding: 1.25rem;
    }

    .nav {
      gap: 1.5rem;
      padding: 0.5rem 0.25rem 1.25rem;
    }

    .nav__links {
      display: flex;
    }

    .brand {
      font-size: 1.15rem;
    }

    .brand__badge {
      width: 34px;
      height: 34px;
    }

    .nav__cta {
      padding: 0.7rem 1.1rem;
      font-size: 1rem;
    }

    .hero {
      display: grid;
      grid-template-columns: minmax(0, 1.05fr) minmax(0, 0.95fr);
      gap: 2.5rem;
      align-items: center;
      padding: 2.5rem;
      border-radius: 28px;
    }

    .hero h1 {
      font-size: clamp(2.6rem, 5vw, 3.8rem);
    }

    .hero__lead {
      font-size: 1.05rem;
      max-width: 36ch;
    }

    .capture,
    .trust {
      max-width: 520px;
    }

    .capture__row {
      grid-template-columns: 1fr auto;
    }

    .trust {
      grid-template-columns: repeat(3, 1fr);
    }

    .hero__visual {
      margin-top: 0;
      gap: 1rem;
    }

    .shot {
      width: min(100%, 240px);
    }

    .features {
      grid-template-columns: repeat(3, 1fr);
      gap: 1rem;
      margin: 1.5rem 0;
    }

    .showcase {
      grid-template-columns: minmax(0, 1.1fr) minmax(0, 1fr);
      gap: 2.5rem;
      align-items: center;
      padding: 2rem;
    }

    .showcase__phones {
      gap: 0.6rem;
    }

    .showcase__copy h2 {
      font-size: clamp(1.9rem, 3.4vw, 2.6rem);
    }

    .final {
      padding: 2.25rem;
      border-radius: 26px;
    }

    .final__inner {
      grid-template-columns: minmax(0, 1.1fr) minmax(0, 1fr);
      align-items: center;
    }

    .final__copy h2 {
      font-size: clamp(1.6rem, 2.6vw, 2rem);
    }

    .final__form {
      grid-template-columns: 1fr auto;
    }

    .final__form .capture__status {
      grid-column: 1 / -1;
    }

    .final__chips {
      display: flex;
      flex-wrap: wrap;
      gap: 1.5rem;
    }
  }
</style>
